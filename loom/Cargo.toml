[package]
name = "loom"
version = "0.1.0"
edition = "2021"

[dependencies]
lattice-daemon = { path = "../lattice-daemon" }
anyhow = "1"
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
